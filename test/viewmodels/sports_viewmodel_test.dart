import 'package:flutter_test/flutter_test.dart';
import 'package:all_bet_info/app/app.locator.dart';

void main() {
  group('SportsViewModel Tests -', () {
    tearDown(() => locator.reset());
  });
}
